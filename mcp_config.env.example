# MCP Browser-Use Configuration File
# Copy this file to .env and fill in your values

# ===========================================
# LLM Provider Configuration
# ===========================================
# Choose your LLM provider: openai, google, anthropic, azure_openai, deepseek, etc.
MCP_LLM_PROVIDER=google
MCP_LLM_MODEL_NAME=gemini-2.0-flash-exp

# Optional: Separate planner LLM (if not set, uses main LLM)
# MCP_LLM_PLANNER_PROVIDER=google
# MCP_LLM_PLANNER_MODEL_NAME=gemini-2.0-flash-exp

# ===========================================
# API Keys (Add the key for your provider)
# ===========================================
# Google AI API Key
GOOGLE_API_KEY=your_google_api_key_here
# MCP_LLM_GOOGLE_API_KEY=your_google_api_key_here

# OpenAI API Key
# OPENAI_API_KEY=your_openai_api_key_here
# MCP_LLM_OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# MCP_LLM_ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Azure OpenAI (if using Azure)
# MCP_LLM_AZURE_OPENAI_API_KEY=your_azure_key_here
# MCP_LLM_AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# MCP_LLM_AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ===========================================
# Browser Configuration
# ===========================================
# Run browser in headless mode (true/false)
MCP_BROWSER_HEADLESS=true

# Disable browser security features (useful for testing, use with caution)
MCP_BROWSER_DISABLE_SECURITY=false

# Keep browser open between requests (improves performance)
MCP_BROWSER_KEEP_OPEN=false

# Optional: Path to Chrome/Chromium binary
# MCP_BROWSER_BINARY_PATH=

# Optional: User data directory for browser profile
# MCP_BROWSER_USER_DATA_DIR=

# Browser window size
MCP_BROWSER_WINDOW_WIDTH=1280
MCP_BROWSER_WINDOW_HEIGHT=1100

# ===========================================
# CDP Connection (Connect to your own browser)
# ===========================================
# Set to true to connect to an existing browser instance
MCP_BROWSER_USE_OWN_BROWSER=false

# Chrome DevTools Protocol URL (if using own browser)
# Launch Chrome with: chrome --remote-debugging-port=9222
# MCP_BROWSER_CDP_URL=http://localhost:9222

# Optional: WebSocket URL (usually auto-detected from CDP)
# MCP_BROWSER_WSS_URL=

# ===========================================
# Agent Tool Configuration
# ===========================================
# Maximum steps the agent can take per task
MCP_AGENT_TOOL_MAX_STEPS=100

# Maximum actions per step
MCP_AGENT_TOOL_MAX_ACTIONS_PER_STEP=5

# Enable vision capabilities (screenshot analysis)
MCP_AGENT_TOOL_USE_VISION=true

# Tool calling method: auto, json_schema, function_calling
MCP_AGENT_TOOL_TOOL_CALLING_METHOD=auto

# Maximum input tokens for LLM context
MCP_AGENT_TOOL_MAX_INPUT_TOKENS=128000

# Enable video recording of browser sessions
MCP_AGENT_TOOL_ENABLE_RECORDING=false

# Optional: Path to save recordings (if enabled)
# MCP_AGENT_TOOL_SAVE_RECORDING_PATH=./recordings

# Optional: Path to save agent history JSON files
# MCP_AGENT_TOOL_HISTORY_PATH=./agent_history

# Override browser headless setting for agent tool (leave empty to use general setting)
# MCP_AGENT_TOOL_HEADLESS=

# Override browser security setting for agent tool
# MCP_AGENT_TOOL_DISABLE_SECURITY=

# ===========================================
# Deep Research Tool Configuration
# ===========================================
# Maximum parallel browser instances for research
MCP_RESEARCH_TOOL_MAX_PARALLEL_BROWSERS=3

# Directory to save research reports and artifacts (REQUIRED)
MCP_RESEARCH_TOOL_SAVE_DIR=./research_output

# ===========================================
# Path Configuration
# ===========================================
# Optional: Directory for downloaded files
# MCP_PATHS_DOWNLOADS=./downloads

# Optional: Directory for Playwright trace files
# MCP_BROWSER_TRACE_PATH=./traces

# ===========================================
# Server Configuration
# ===========================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
MCP_SERVER_LOGGING_LEVEL=INFO

# Optional: Log file path (empty = stdout)
# MCP_SERVER_LOG_FILE=./mcp_server.log

# Enable anonymized telemetry
MCP_SERVER_ANONYMIZED_TELEMETRY=true

# Optional: MCP client config (JSON string for internal controller)
# MCP_SERVER_MCP_CONFIG=

# ===========================================
# HTTP Server Configuration (for integration)
# ===========================================
# Port for the HTTP bridge server
PORT=5001

# Host address for the HTTP bridge server
HOST=127.0.0.1

# Optional: Custom path to mcp-browser-use directory
# MCP_BROWSER_USE_DIR=./mcp-browser-use

# URL of the mcp-server-browser-use HTTP server. Proxy and Flask bridge connect here. Start the server with: uv run mcp-server-browser-use server (in mcp-browser-use directory).
# MCP_BROWSER_USE_HTTP_URL=http://127.0.0.1:8383/mcp

