# Core web framework and server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# HTTP client library
httpx>=0.25.0

# Environment variable management
python-dotenv>=1.0.0

# Flask and CORS (for MCP browser server)
flask>=3.0.0
flask-cors>=4.0.0

# Telegram bot integration
python-telegram-bot>=20.0

# Testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Numerical computing (for vector store)
numpy>=1.24.0

# File operations libraries
python-docx>=1.1.0
openpyxl>=3.1.0
PyPDF2>=3.0.0
Pillow>=10.0.0
reportlab>=4.0.0

# Cryptographic operations (for HTTPS server)
cryptography>=41.0.0

# MCP (Model Context Protocol) client library
# Note: Package name may be 'mcp' or 'mcp-sdk' - verify with: pip search mcp
mcp>=0.1.0
